{
  "name": "richard-hein-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:pages": "vite build && node ../../vibecoded/scripts/build-vibecoded-only.mjs",
    "preview": "vite preview",
    "deploy": "npm run build:pages && npx wrangler pages deploy public --project-name=richard-hein-portfolio"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.0.0"
  }
}
